"""
SwinJSCC-compatible utility layers for MambaVision_JSCC.

This package provides a minimal subset of the original SwinJSCC
`net` package (patch embedding/merging and channel models) so that
`mamba_jscc` can run without depending on an external SwinJSCC repo.
"""

